header
    a(href="/").header__logo
        i.fab.fa-youtube
    nav
        ul
            li
                a(href="/search")
                    i.fas.fa-search
                if !login 
                    li 
                        a(href="/join") Join
                    li 
                        a(href="/login").header__btn Login
                else 
                    li
                        a(href="/videos/uploadvideo") Upload video
                    li
                        a(href="/users/edit") Edit Profile
                    li 
                        a(href="/users/logout") Logout 
                    li
                        a(href=`/users/${user._id}`)
                            if user.avatar_url===""
                                span 😊
                            else
                                if user.githublogin
                                    img.header_avatar(src=user.avatar_url)
                                else
                                    img.header_avatar(src="/"+user.avatar_url)
